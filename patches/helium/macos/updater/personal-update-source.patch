--- a/components/helium_services/helium_services_helpers.cc
+++ b/components/helium_services/helium_services_helpers.cc
@@ -28,9 +28,9 @@
 #endif
 
 #if defined(UPDATES_SUPPORTED)
-constexpr std::string_view kUpdateBaseUrl = "https://updates.helium.computer/";
+constexpr std::string_view kUpdateBaseUrl = "https://github.com/RTLiang/helium-macos/releases/download/personal-updates/";
 constexpr const char kUpdatePath[] =
 #if BUILDFLAG(IS_MAC)
-    "mac/appcast-" CPU_ARCH_STR ".xml";
+    "appcast-" CPU_ARCH_STR ".xml";
 #elif BUILDFLAG(IS_WIN)
     "win/appcast.xml";
 #endif
--- a/build/apple/tweak_info_plist.py
+++ b/build/apple/tweak_info_plist.py
@@ -146,6 +146,17 @@
         return False
     values = dict(zip(('MAJOR', 'MINOR', 'BUILD', 'PATCH'), groups))
 
+    values['PERSONAL_BUILD_NUMBER'] = '0.0'
+    with open(os.path.join(TOP, 'chrome/VERSION'), encoding='utf-8') as file:
+        for line in file:
+            name, _, value = line.strip().partition('=')
+            if name == 'PERSONAL_BUILD_NUMBER':
+                if not re.fullmatch(r'[0-9]+\.[0-9]+', value):
+                    print('Invalid personal build number', file=sys.stderr)
+                    return False
+                values[name] = value
+                break
+
     for key in version_format_for_key:
         plist[key] = _GetVersion(version_format_for_key[key], values, overrides)
 
@@ -528,7 +539,7 @@
             # http://lists.apple.com/archives/carbon-dev/2006/Jun/msg00139.html
             # BUILD will always be an increasing value, so BUILD_PATH gives us
             # something unique that meetings what LS wants.
-            'CFBundleVersion': '@MAJOR@.@MINOR@.@BUILD@.@PATCH@',
+            'CFBundleVersion': '@MAJOR@.@MINOR@.@BUILD@.@PATCH@.@PERSONAL_BUILD_NUMBER@',
         }
     else:
         version_format_for_key = {
